# 權限

| 權限節點 | 預設 | 說明 |
|----------|------|------|
| `loreveil.lore.toggle` | true | 允許使用 `/lore` 切換物品描述顯示 |

- 預設**所有玩家**皆可使用；若要限制，用權限插件收回即可。

收回範例（LuckPerms）：

```
lp group default permission set loreveil.lore.toggle false
```
